<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.haris.weitani.moviecataloguesub2">

    <uses-permission android:name="android.permission.INTERNET" />
    <permission android:name="com.haris.weitani.moviecataloguesub2.content_provider" android:protectionLevel="normal"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".notification.NotificationSettingsActivity"></activity>
        <activity android:name=".favorite_activity.FavoriteActivity" />
        <activity android:name=".detail_view.TvShowDetailView" />
        <activity android:name=".detail_view.MovieDetailView" />
        <activity android:name=".main_activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <receiver
            android:name=".notification.NotificationReceiver"
            android:enabled="true"
            android:exported="true" />
        <receiver android:name=".widget.FavoriteBannerWidget">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>

            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/favorite_banner_widget_info" />
        </receiver>

        <service
            android:name=".widget.FavoriteWidgetService"
            android:permission="android.permission.BIND_REMOTEVIEWS" />

        <provider
            android:name=".content_provider.FavoriteMovieContentProvider"
            android:authorities="com.haris.weitani.moviecataloguesub2.content_provider"
            android:multiprocess="true"
            android:enabled="true"
            android:exported="true"
            />
    </application>

</manifest>